name: Build and Deploy

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Build and Deploy
      run: |
        # Copiar archivos al directorio de implementación
        cp -R * $GITHUB_WORKSPACE/deploy

        # Imprimir la lista de archivos copiados
        ls $GITHUB_WORKSPACE/deploy

